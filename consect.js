const directories = {
    home: '/user/home',
    documents: '/user/documents',
    downloads: '/user/downloads'
};

const myObject = {
    directories: directories,
    // other properties
};

console.log(myObject);
